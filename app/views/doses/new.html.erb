
<%= simple_form_for [ @cocktail, @dose ] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, as: :select, prompt: "Choose an ingredient" %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
